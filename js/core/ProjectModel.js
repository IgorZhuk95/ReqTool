/**
 * Project class
 * @param project
 * @constructor
 */
function ProjectModel(project) {
    var self = this;
    self.id = project.id;
    self.name =project.name;
    self.reqUrl = project.reqUrl;
}
